{
  "name": "ci-cd-tutorial",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node -e \"const fs = require('fs'); fs.mkdirSync('dist', { recursive: true }); const files = fs.readdirSync('src'); files.forEach(file => fs.copyFileSync('src/' + file, 'dist/' + file));\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "eslint": "^9.39.2",
    "prettier": "^3.7.4"
  }
}
